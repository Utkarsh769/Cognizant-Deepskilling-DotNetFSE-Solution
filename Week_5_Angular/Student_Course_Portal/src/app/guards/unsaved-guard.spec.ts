import { TestBed } from '@angular/core/testing';

import { unsavedGuard } from './unsaved-guard';

describe('unsavedGuard', () => {

  it('should be created', () => {

    expect(unsavedGuard).toBeTruthy();

  });

});